## 算术与逻辑运算

逻辑变量之间的运算称为逻辑运算。二进制数1和0在逻辑上可以代表“真”与“假”。

|逻辑运算类型|符号|运算规则|
|---|---|---|
|逻辑与（且）|&&、*、.、∩、∧、AND|连接的两个逻辑值全为1时，结果才取1，否则取0|
|逻辑或|+、∪、∨、OR|连接的两个逻辑值全为0时，结果才取0，否则取1|
|逻辑非|!、¬、~、NOT、ˉ|将原逻辑值取反即可（1变0，0变1）|
|逻辑异或|⊕、XOR|连接的两个逻辑值不相同时取1，数值相同时取0|
|优先次序（从高到低）|!（非） > &&（与） > 异或 > （或）||